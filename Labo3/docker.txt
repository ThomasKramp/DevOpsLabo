sudo docker run -d --port 127.0.0.1:3306:3306 -v /data/mariadb:/var/lib/mysq --name "db-kramp" -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=dbthomas -d mariadb:tag

sudo docker run -d --name "wp-kramp" -p 8080:80 -v /data/wordpress:/var/www/html -e WORDPRESS_DB_HOST=db-kramp -e WORDPRESS_DB_PASSWORD=root -e WORDPRESS_DB_NAME=dbthomas -e WORDPRESS_DB_USER=kramp wordpress

docker ps

docker ps -a

docker inspect

docker logs wp-thomaskramp

3 Containers:
	mongo
	jenkins
	logstash
