sudo docker run -d --expose 3306 -v /data/mariadb:/var/lib/mysql --name 
"db-kramp" -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=dbthomas -d 
mariadb

sudo docker run -d --name "wp-kramp" -p 8080:80 -v 
/data/wordpress:/var/www/html -e WORDPRESS_DB_HOST=db-kramp -e 
WORDPRESS_DB_PASSWORD=root -e WORDPRESS_DB_NAME=dbthomas -e 
WORDPRESS_DB_USER=kramp wordpress

docker exec -it db-kramp mysql -u root -p
password: root

docker network create --attachable wp-network
docker network connect wp-network db-kramp
docker network connect wp-network wp-kramp

docker ps

docker ps -a

docker inspect

docker logs wp-thomaskramp

3 Containers:
	mango (manga)
	Pokemon Go Map
	Minecraft
